# 大喜利（oogiri-game）

自動生成されたお題に、各自がフリップ（手書き＋文字）で答えるパーティーゲーム。
点数を計算するゲームではない。アプリはお題の供給・フリップの配布・採点の集計だけを担当する。

**仕様は `docs/SPEC.md`。お題生成は `docs/TOPIC-GEN.md`。ルールの話をするときは必ず先に読む。**

## コマンド

**このディレクトリで実行する。** 親フォルダで叩くと ENOENT になる。

| コマンド | 内容 |
|---|---|
| `npm.cmd start` | 本番相当。`dist/` を配信するサーバー。http://localhost:3600 |
| `npm.cmd run build` | クライアント（`dist/`）とサーバー（`dist-server/`）の両方 |
| `npm.cmd run dev` | Vite 開発サーバー（5175）。**クライアントのみ＝部屋は作れない** |
| `npm.cmd test` | Vitest 81件（判定10 / ルール25 / viewFor 11 / 検証19 / 生成16） |
| `npm.cmd run topics:preview` | お題を50問出して読む（`--n 50 --seed 1 --brought`） |

**オンラインの動作確認に `npm run dev` を使わない。** 部屋が作れない。
`npm.cmd run build && npm.cmd start` で立てて、**複数タブで実際に触る。**

## デプロイ

リモートは `github.com/ajajax1212/oogiri`（**リポジトリ名は `oogiri`**。ディレクトリ名と違う）。
GitHub → Render の無料プラン。`render.yaml` を同梱してある。

- **push は自分の判断でしてよい。** ただし **push すると Render が自動でビルドして本番に出る**ので、
  先に `npm.cmd run build` と `npm.cmd test` を通してから push する。
- **ビルドは `npm ci --include=dev`。** Render は `NODE_ENV=production` を既定で入れるので、
  これを明示しないと `vite` と `tsc` が入らないままビルドに入って落ちる。
- 無料プランなのでスリープする。しばらく触っていないと初回だけ30秒ほど待たされる。
- 部屋の状態はメモリにしかない。インスタンスが落ちれば進行中のゲームは消える（許容している）。
- push 前に `public/` を追跡し損ねていないか見る。抜けると**本番だけ音が404**になる。

## 構造

```
src/engine/   ゲームのルール。React も Socket.IO も知らない純粋な層
  types.ts    Flip / Player / GameState / Action / Topic
  judge.ts    採点の集計。整数のまま判定する
  topic.ts    お題の生成。データは引数で受け取る（ファイルを読まない）
  reducer.ts  ルールの本体。お題の生成は ctx.nextTopic() として外から渡す
  view.ts     viewFor — 1人分に絞った状態
src/net/
  events.ts   EV の唯一の定義 + 入力の検証
  useRoom.ts  接続・再接続・状態受信
src/ui/       App / Lobby / Game / FlipEditor / Flip / TopicForm / sound.ts / styles.css
server/
  index.ts    Socket.IO のハンドラ。reducer と viewFor を呼ぶだけ
  rooms.ts    部屋・席・token・タイマー。ルールは持たない
  topicData.ts  素材ファイルの読み込み（engine を純粋に保つため server 側）
data/topics/  patterns.json（骨格60本）と words/*.json（素材）
public/sfx/   配る音源。生の置き場は sound/（追跡外）
```

### 層の境界を守る

- **`src/engine/` にネットワークと React を持ち込まない。**
- **ルールの変更は `reducer.ts` だけで完結させる。** サーバーに条件分岐を足すと、
  reducer のテストが通っても本番が違う動きをする。
- **`reducer.ts` はお題を作らない。** 乱数と素材ファイルを抱えると、
  同じ状態から同じ結果が出なくなってテストが書けない。
- **`server/rooms.ts` はゲームのルールを知らない。** 席・token・接続状態・時計だけ。

## このゲーム固有の注意

- **回答権は先着1人。順番待ちの列は作らない。** 押した人が終わったら全員のボタンが復活する。
- **合意ボタンは1つだけ。** 回答が0件なら「引き直す」、1件以上なら「次のお題に行く」。
  2つ並べると票が割れてどちらも全員に届かず膠着する。
- **ready を解除するのは「最初の」公開のときだけ。** そこでボタンの文字が
  「引き直す」から「次のお題に行く」へ変わるので、前の意味で押した印を流用しない。
  2人目以降の公開では**残す**。回答のたびに押し直させない（2026-08-31 に変更）。
  回答した本人の印も外さない。
- **フリップを消すのは「お題が変わったとき」と「自分の回答の判定が終わったとき」だけ。**
  お題の**文**で判定してはいけない。宣言の間は文が伏せられるので、
  誰かが回答するたびに全員の書きかけが消える（一度これで壊した）。
- **フリップの板では、押したときの既定動作を `mousedown` で止める**（`pointerdown` ではない）。
  止めないとブラウザが焦点を body へ移し、こちらが textarea に当てた焦点が直後に外れて
  `onBlur` の `dropEmpty` が出来たての箱を捨てる（箱が一瞬で消える）。
  **`pointerdown` で止めてはいけない。** Chrome が click も dblclick も発行しなくなり、
  書き直しのダブルクリックが黙って死ぬ。
- **ダブルクリックの受け口は板に置く。箱に付けても呼ばれない。**
  `setPointerCapture` を張った時点で dblclick の宛先が板へ移るため。
  どの箱かは直前の `pointerdown` が選んだ `sel` で分かる。
- **書きかけのフリップはサーバーに置かない**（`sessionStorage`）。配るのは
  「書いているか」の真偽値だけ。
- **席の合鍵（token）も `sessionStorage`。** `localStorage` だと同じブラウザの
  2つ目のタブが先の席を奪う。複数タブで動作確認するので必ず踏む。
- **`viewFor` は `history` を丸ごと落とす。** 型・骨格・語尾・素材の内訳が漏れると
  次のお題を計算できてしまう。乱数シードも同じ。
- **配信に「増え続けるもの」を載せない。** これで3回失敗している。
  溜まった控えを state に相乗り → 「書いています」のたびに全フリップが再送。
  次に「増えたときだけ配列まるごと」→ 1件増えるたびに全部を送り直し、
  回答数の2乗で増えた（実測: 40回・6人で判定のたびに 2.1MB）。
  正解は**増えたぶんだけ送って受け手が継ぎ足す**。
- **同じ中身を二度配らない。** 公開されたフリップは鍵（`flipKey`）を付け、
  一度渡した相手には載せない（`server/index.ts` の `thinFlip`）。
  実測で1回の公開につき10通中9通が無駄だった。
- **装飾のための状態で全体配信をしない。** 「書いています」は真偽値1つなので
  `EV.writing` だけを配る。state を配ると6人で毎秒3.8回、全員の画面が丸ごと作り直される。
- **演出に乱数を使うときは、全員の画面で同じ値になるようにする**（`src/ui/closing.ts` の `seedOf`）。
  各画面で `Math.random()` を呼ぶと、同じ部屋なのに人によって判定の出る瞬間がずれる。
  種は回答者の id とお題の id から作る。
- **採点は1人1回、変更も取り消しもできない。** 押し直せると他人の押した数を見て動かせる。
- **時計はサーバーが持つ**（`rooms.ts` の `schedule`）。クライアントは `deadline` を見るだけ。
  ただし **`deadline - Date.now()` は端末の時計を信じている**。遅れている端末では
  残りが長く出るので、画面の尺に使うときは必ず `DELAY` で頭を押さえる。
  押さえないと、お題の進捗の線が途中のまま画面が切り替わる。
- **満点大笑いの回数以外の得点を持たない。** 順位を出すと面白さの判断が数字に寄る。
- **投稿されたお題の本文は、出した本人にしか配らない。** 件数だけ全員に見せる。
  出るまで知らないから面白い（TOPIC-GEN.md §9.1）。
- **効果音は音源ファイルを鳴らし、読めなければ合成音に落ちる。**
  `src/ui/sound.ts` の合成音（tone / noise / taiko / metal / bell と `synth`）は**消さない**。
  取得に失敗してもオフラインでも鳴らすための保険。**既定はオフで、音を入れた操作の
  あとで初めて取りに行く**（開いただけでは読まない）。音量は `SFX` の表と `MASTER` で決め、
  素材ごとに素のピークが違うので `gain` は音ごとに別。全体を動かすときは `MASTER` だけ。
- **`sound/` は追跡外**（生のダウンロード置き場）。配るのは `public/sfx/` の方だけ。
- **フリップの書体は Web フォント**（`index.html` の Google Fonts）。端末の書体に頼ると、
  **書いた人の画面と見る人の画面で別の書体になる**。筆で書いた一言が相手には明朝で届き、
  同じ絵が全員に届かない。日本語だが unicode-range で小分けに配られるので、
  実際に落ちるのは使った字の分だけ。`display=swap` で届くまでは端末の書体で読める。
- **「書いています」は消すほうだけ遅らせる**（`useRoom.ts` の `WRITING_LINGER`）。
  ペンは点を打つたびに pointerdown/up を繰り返すので、素で流すと全員の画面で点滅する。
- **ホストは抜けたら次の人へ渡る**（`reducer.ts` の `handoffHost`）。渡さないと
  「始める」を押せる人が居なくなる。**繋がっている中でいちばん先に座った人**へ。
  乱数で選ぶと同じ状態から同じ結果が出ずテストが書けない。
- **前回オンにしていた人のために、最初の操作で音源の取得も始める**（`sound.ts` の `prime`）。
  AudioContext を作るだけにすると fetch が一度も走らず、差し替えた音源が
  永久に鳴らずに合成音のままになる（実際にこれで「音が反映されない」と言われた）。
- **AudioContext はユーザー操作の中でしか作らない。** ブラウザが自動再生を止めるので、
  音を入れるトグルを押した瞬間が唯一の機会。
- **音は「場面が変わったとき」だけ鳴らす。** 「今この phase だから鳴らす」にすると、
  再接続やリロードで状態を受け直しただけで鳴る。
- お題の素材と骨格を触るときは `docs/TOPIC-GEN.md` §5.2 の役割（roles）を読む。
  `cat` と `tags` だけでは文法が守れない。

## やってはいけないこと

- **ルールやバランスを勝手に変えない。** 面白さの判断は本人がする。
  実装上おかしいと思ったら、直す前に指摘する。
- `dist/` `dist-server/` をコミットしない。
- **GitHub にリポジトリを勝手に作らない。** 新規リポジトリの作成は本人が GitHub 上でやる。
